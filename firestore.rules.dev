rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // REGLAS DE DESARROLLO - COMPLETAMENTE PERMISIVAS
    // ⚠️ NUNCA USAR EN PRODUCCIÓN ⚠️
    
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
